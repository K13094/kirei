version: '3.8'

services:
  kirei-nail-bar:
    build: .
    container_name: kirei-nail-bar
    restart: unless-stopped
    ports:
      - "3000:80"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
